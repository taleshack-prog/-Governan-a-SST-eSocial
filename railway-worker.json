{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.api"
  },
  "deploy": {
    "startCommand": "celery -A api.tasks.celery_app.app worker --loglevel=info --concurrency=2",
    "restartPolicyType": "ON_FAILURE"
  }
}
